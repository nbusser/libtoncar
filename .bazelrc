build --incompatible_enable_cc_toolchain_resolution

build --compilation_mode=opt
build --platforms=//platform:gba

build --copt=-fdiagnostics-color
build --host_copt=-fdiagnostics-color

try-import %workspace%/.bazelrc.user
