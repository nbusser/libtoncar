# bazel run --config=host --config=strict //tools/generate_compile_commands -- "//"
alias(
    name = "generate_compile_commands",
    actual = "@wolfd_bazel_compile_commands//:generate_compile_commands",
)
